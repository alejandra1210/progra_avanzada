//Modulo


    const areaTriangulo =(a,b) => (a+b)/2;

    const areaCuadrado = (a,b)=> a*b;
    
     const areaCirculo = (a) => a*a 





module.exports = {
    areaTriangulo,
    areaCuadrado,
    areaCirculo
}